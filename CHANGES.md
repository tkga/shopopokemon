# เพิ่มระบบ "รหัสสินค้า" (แบบ B — ผูกกับชนิดสินค้า)

## สิ่งที่ได้
- สินค้าชื่อ+ประเภทเดียวกัน (เช่น Charizard Shiny) จะได้รหัสเดียวกันเสมอ ไม่ว่าจะอยู่ไอดีไหน เช่น `A014`
- หน้า **catalog.html** (ที่ลูกค้าเห็น) โชว์ป้าย "รหัส A014" บนการ์ดสินค้าทุกใบ
- ในแอปจัดการร้าน หน้า **ไอดีเกม** มีปุ่มใหม่ **"ค้นหาด้วยรหัสสินค้า"** — พิมพ์รหัสที่ลูกค้าส่งมา ระบบจะโชว์ทุกไอดีที่มีของชิ้นนี้ พร้อมจำนวนคงเหลือ กดแล้วเปิดฟอร์มแก้ไข/ตัดสต๊อกได้ทันที ไม่ต้องไล่เปิดทีละไอดีอีกแล้ว
- รหัสถูกสร้างอัตโนมัติตอนเพิ่ม/แก้ไขสต๊อก และของเก่าที่มีอยู่แล้วก่อนอัปเดตจะได้รับรหัสอัตโนมัติทันทีที่เปิดแอปครั้งแรกหลังอัปเดต (migration)

## ไฟล์ที่แก้/เพิ่ม
- `src/utils.js` — ฟังก์ชันสร้าง/ค้นหารหัส (`ensureProductCode`, `lookupProductCode`, `findStockByProductCode`) + เก็บทะเบียนรหัสไว้ที่ `data.productCodes`
- `src/App.jsx` — ผูกรหัสให้ตอนบันทึกสต๊อก (`saveStock`) + เพิ่มคอลัมน์ "รหัสสินค้า" ในไฟล์ Export Excel
- `src/components/StockModal.jsx` — โชว์รหัส (หรือพรีวิวรหัสที่จะได้) ตอนเพิ่ม/แก้ไขสต๊อก
- `src/components/AccountsTab.jsx` — ปุ่ม "ค้นหาด้วยรหัสสินค้า"
- `src/components/CodeSearchModal.jsx` — **ไฟล์ใหม่** หน้าค้นหาด้วยรหัส
- `src/googleSync.js` — เพิ่มคอลัมน์ "รหัสสินค้า" ต่อท้ายชีต Stock (ไม่แทรกกลาง กันคอลัมน์เดิมเลื่อน)
- `catalog.html` — อ่านคอลัมน์รหัสจากชีต โชว์เป็นป้ายบนการ์ดและในหน้ารายละเอียดสินค้า

## ต้องทำหลังอัปเดต
ถ้าร้านซิงก์ Google Sheet อยู่แล้ว ให้กด **ซิงก์ใหม่อีกครั้ง** (หน้าตั้งค่า) หนึ่งครั้งหลังอัปเดต เพื่อให้คอลัมน์ "รหัสสินค้า" ถูกเพิ่มเข้าไปในชีต Stock และ catalog.html จะเริ่มโชว์รหัสได้

## ตรวจสอบแล้ว
รัน `npm install && npm run build` ผ่านทั้งโปรเจกต์ ไม่มี error
